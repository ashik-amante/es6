// use of backtic

const a  = 10;
const b = 12;
console.log(`the sum of ${a} and ${b} is ${a+b}`);

const message = ` hello
bal falo`;
console.log(message);

const number = [12,23,34,45,56]
const student = {
    name: 'ashik',
    age: 10

}

const output = `the message is ${student.name}'s age is ${number[1]} `
console.log(output);